[package]
name = "mew-harness"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Shared markdown-directory loader and discovery helpers used by mew-skills, mew-personas, and mew-subagents."

[dependencies]
tracing = { workspace = true }

[dev-dependencies]
tempfile = "3"

[lib]
path = "src/lib.rs"
